#!/usr/python3.5
# modulo para ilustrar como sao feitas as importacoes

def media (lista):
    """Doc funcao que recebe uma lista numerica e retorna a media aritmetica dos elementos """
    return float(sum(lista)/len(lista))
